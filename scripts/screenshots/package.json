{
  "name": "devops-loop-screenshots",
  "version": "1.0.0",
  "description": "Playwright screenshot automation for DevOps Loop tutorial",
  "private": true,
  "scripts": {
    "capture":      "node capture.js",
    "capture:dry":  "node capture.js --dry-run",
    "generate-map": "node ../generate-image-map.js",
    "rename":       "node ../rename-images.js",
    "rename:dry":   "node ../rename-images.js --dry-run"
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "playwright": "^1.44.0"
  }
}
